# Console Memory

Console-owned operating memory lives here.

Use this directory for Console current context, task state, handoff summaries, and reboot notes.

Project implementation docs still belong in the relevant project repo, such as `asciiscope/docs/` or `asciiscope-clap/`.

See `CODEGUIDE/protocols/agent_memory.md`.
